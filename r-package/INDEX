Initialize        A function which initializes the package; this must be called
                  first otherwise the other functions will not work.
Login             Establishes a session with Thomson Reuters, for instance; for
                  example, "Login('YouCos_DACSID')".
Logout            Terminates the session with Thomson Reuters; for example
                  "Logout()".
Query             Query Thomson Reuters for market price updates for the RICS
                  provided; for example "Query('MSFT.O', 'GOOG.O')".
GetNextUpdateAsJavaObject    Function returns the next market price update Java object reference.
GetNextUpdate     Function returns the next market price update as a
                  data.frame; for example "next <- GetNextUpdate()".
GetTimeSeriesDataFor     Function returns the time series data for the given
                         RIC; for example "timeSeries <- cdatafeedtre::GetTimeSeriesDataFor("ILMN.O", "daily")"
About             Function prints some information about this package.
